# Problem 261

See `solution.tex` for the write-up.

Main conclusions:

- There are infinitely many such \(n\).
- In fact, every positive integer \(n\) has such a finite distinct representation.
- There is a rational \(x\), for instance \(x=1/2\), with continuum-many distinct infinite representations.
